version: 2
jobs:
  build:
    working_directory: ~/league-app
    docker:
      - image: circleci/node:6-browsers
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
      - run:
          name: install-dependencies
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
          paths:
            - ~/.cache/npm
            - ./node_modules
      - run:
          name: unit-test
          command: npm run test -- --single-run --no-progress
      - store_artifacts:
          path: test-results.xml
      - run:
          name: prod-build
          command: npm run build --prod --aot --no-progress
